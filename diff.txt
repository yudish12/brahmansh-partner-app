diff --git a/lib/controllers/chatController.dart b/lib/controllers/chatController.dart
index de28c54..5640eaf 100644
--- a/lib/controllers/chatController.dart
+++ b/lib/controllers/chatController.dart
@@ -429,6 +429,48 @@ class ChatController extends GetxController
     }
   }
 
+  /// Upload voice recording to Firebase Storage and send as chat message (WhatsApp-style).
+  Future<void> sendVoiceMessageToFirebase(
+    String chatId,
+    int partnerId,
+    File audioFile,
+  ) async {
+    try {
+      isUploading = true;
+      update();
+      final storageReference = FirebaseStorage.instance.ref().child(
+          '$chatId/voice_${DateTime.now().millisecondsSinceEpoch}.m4a');
+      final uploadTask = await storageReference.putFile(audioFile);
+      if (uploadTask.state == TaskState.success) {
+        debugPrint('Voice message uploaded');
+      }
+      final String downloadURL = await storageReference.getDownloadURL();
+      debugPrint('Voice message URL: $downloadURL');
+      final ChatMessageModel chatMessageModel = ChatMessageModel(
+        createdAt: DateTime.now(),
+        updatedAt: DateTime.now(),
+        isDelete: false,
+        isRead: true,
+        userId1: '${global.currentUserId}',
+        userId2: '$partnerId',
+        message: 'Voice message',
+        attachementPath: downloadURL,
+        isEndMessage: false,
+      );
+      await uploadMessage(chatId, '$partnerId', chatMessageModel);
+    } catch (e) {
+      log('Upload voice exception: $e');
+      global.showToast(
+        message: 'Failed to send voice message',
+        textColor: Colors.white,
+        bgColor: Colors.red,
+      );
+    } finally {
+      isUploading = false;
+      update();
+    }
+  }
+
   storedefaultmessage(String msg) async {
     try {
       await global.checkBody().then(
@@ -569,50 +611,86 @@ class ChatController extends GetxController
   bool isfreechatwithbalance = false;
   int amountisfreechat = 0;
   String msgisfreechat = '';
+  /// Set true when chat request API returns success; intake screen uses this to show success dialog/toast.
+  bool chatRequestSuccess = false;
 
   sendChatRequest(int astrologerId, bool isFreeSession, String time) async {
+    chatRequestSuccess = false;
     try {
       await apiHelper
           .sendAstrologerChatRequest(astrologerId, isFreeSession, time)
           .then((response) {
-        dynamic rspnse1 = json.decode(response.body)['status'];
-        log('chat response status ${rspnse1}');
+        if (response?.body == null || response.body.isEmpty) {
+          chatRequestSuccess = false;
+          global.showToast(
+            message: 'Request failed. Please try again.',
+            textColor: whiteColor,
+            bgColor: blackColor,
+          );
+          update();
+          return;
+        }
+        final body = json.decode(response.body);
+        dynamic rspnse1 = body['status'];
+        log('chat response status $rspnse1');
         log('chat response type ${rspnse1.runtimeType}');
 
-        if (rspnse1 == 200) {
+        // Success: status can be true (boolean) or 200 (legacy)
+        final isSuccess = rspnse1 == true || rspnse1 == 200;
+        if (isSuccess) {
+          chatRequestSuccess = true;
           isfreechatwithbalance = false;
-          update();
-          // isfreechatwithbalance=false;
+          final message = body['message']?.toString() ?? 'Chat request sent successfully.';
+          final offerApplied = body['offerApplied'] == true;
+          final successMsg = offerApplied
+              ? '$message Offer applied!'
+              : message;
           global.showToast(
-            message: 'Sending chat request..',
+            message: successMsg,
             textColor: global.textColor,
             bgColor: global.toastBackGoundColor,
           );
+          update();
         } else if (rspnse1 == 400) {
-          if (json.decode(response.body).containsKey('recordList') &&
-              json.decode(response.body)['recordList'] != null) {
-            String msg = json.decode(response.body)['recordList']["message"];
+          chatRequestSuccess = false;
+          if (body.containsKey('recordList') && body['recordList'] != null) {
+            String msg = body['recordList']["message"];
 
-            dynamic amount =
-                json.decode(response.body)['recordList']["minAmount"];
+            dynamic amount = body['recordList']["minAmount"];
             log('chat amount $amount');
             isfreechatwithbalance = true;
             amountisfreechat = amount;
             msgisfreechat = msg;
           } else {
-            String msg = json.decode(response.body)['message'];
-
+            String msg = body['message']?.toString() ?? 'Request failed.';
             global.showToast(
-                message: '$msg', textColor: whiteColor, bgColor: blackColor);
+                message: msg, textColor: whiteColor, bgColor: blackColor);
             Get.back();
             Get.back();
           }
-
           update();
-        } else {}
+        } else {
+          chatRequestSuccess = false;
+          final message = body['message']?.toString();
+          global.showToast(
+            message: (message != null && message.isNotEmpty)
+                ? message
+                : 'Something went wrong. Please try again.',
+            textColor: whiteColor,
+            bgColor: blackColor,
+          );
+          update();
+        }
       });
     } catch (e) {
-      print('Exception in sendCallRequest : - ${e.toString()}');
+      chatRequestSuccess = false;
+      print('Exception in sendChatRequest : - ${e.toString()}');
+      global.showToast(
+        message: 'Request failed. Please try again.',
+        textColor: whiteColor,
+        bgColor: blackColor,
+      );
+      update();
     }
   }
 
diff --git a/lib/utils/config.dart b/lib/utils/config.dart
index e60e295..691b8d2 100644
--- a/lib/utils/config.dart
+++ b/lib/utils/config.dart
@@ -4,10 +4,10 @@ String ISCHATAVILABLE = "ISCHATAVILABLE";
 String privacyUrl = "https://brahmanshtalk.com/privacy-and-policy";
 String termsconditionUrl = "https://brahmanshtalk.com/terms-and-condition";
 String refundpolicy = 'https://brahmanshtalk.com/refundPolicy';
-String baseUrl = "https://phpstack-1555706-6173918.cloudwaysapps.com/api";
-String websiteUrl = "https://phpstack-1555706-6173918.cloudwaysapps.com/";
-String webBaseUrl = "https://phpstack-1555706-6173918.cloudwaysapps.com/api/";
-String imgBaseurl = "https://phpstack-1555706-6173918.cloudwaysapps.com/";
+String baseUrl = "https://brahmanshtalk.com/api";
+String websiteUrl = "https://brahmanshtalk.com/";
+String webBaseUrl = "https://brahmanshtalk.com/api/";
+String imgBaseurl = "https://brahmanshtalk.com/";
 
 String appMode = "LIVE";
 Map<String, dynamic> appParameters = {
diff --git a/lib/utils/global.dart b/lib/utils/global.dart
index 530b2f9..02dad14 100644
--- a/lib/utils/global.dart
+++ b/lib/utils/global.dart
@@ -96,6 +96,8 @@ dynamic couponmycode;
 bool freeRequestMinimum = false;
 int amountisfreechat = 0;
 int amountisfreecall = 0;
+/// True when first call offer API returned at least one offer (used for Call/Chat intake UI).
+bool firstCallOfferAvailable = false;
 
 Future<String> playstoreUrl() async {
   PackageInfo packageInfo = await PackageInfo.fromPlatform();
diff --git a/lib/utils/services/api_helper.dart b/lib/utils/services/api_helper.dart
index 4b8557e..91e5a07 100644
--- a/lib/utils/services/api_helper.dart
+++ b/lib/utils/services/api_helper.dart
@@ -71,6 +71,7 @@ import '../../model/rec_model.dart';
 import '../../model/referalmodel.dart';
 import '../../views/chatwithAI/models/ai_chatid_Model.dart';
 import '../../views/poojaBooking/model/faqmodel.dart';
+import '../../model/first_call_offer_model.dart';
 import '../config.dart';
 
 class APIHelper {
@@ -4314,4 +4315,29 @@ class APIHelper {
       debugPrint('Exception:- in updateMin:- ' + e.toString());
     }
   }
+
+  /// Fetches first call offers. Returns list of offers (minutes, price) or empty list on failure.
+  Future<List<FirstCallOffer>> getFirstCallOffer() async {
+    try {
+      final response = await http.get(
+        Uri.parse('$baseUrl/firstCalloffer'),
+        headers: await global.getApiHeaders(true),
+      );
+      log('getFirstCallOffer response: ${response.body}');
+      if (response.statusCode == 200) {
+        final decoded = json.decode(response.body);
+        if (decoded['status'] == true && decoded['data'] != null) {
+          final list = decoded['data'] as List<dynamic>?;
+          if (list != null && list.isNotEmpty) {
+            return list
+                .map((e) => FirstCallOffer.fromJson(e as Map<String, dynamic>))
+                .toList();
+          }
+        }
+      }
+    } catch (e) {
+      log('Exception in getFirstCallOffer(): $e');
+    }
+    return [];
+  }
 }
diff --git a/lib/views/chat/AcceptChatScreen.dart b/lib/views/chat/AcceptChatScreen.dart
index f7fe300..39d8d1a 100644
--- a/lib/views/chat/AcceptChatScreen.dart
+++ b/lib/views/chat/AcceptChatScreen.dart
@@ -4,6 +4,10 @@ import 'dart:async';
 import 'dart:developer';
 import 'dart:io';
 import 'package:AstrowayCustomer/controllers/history_controller.dart';
+import 'package:path_provider/path_provider.dart';
+import 'package:record/record.dart';
+import 'package:permission_handler/permission_handler.dart';
+import 'package:audioplayers/audioplayers.dart';
 import 'package:AstrowayCustomer/views/addMoneyToWallet.dart';
 import 'package:AstrowayCustomer/views/chat/endDialog.dart';
 import 'package:AstrowayCustomer/views/chat/pdfviewerpage.dart';
@@ -90,8 +94,27 @@ class _AcceptChatScreenState extends State<AcceptChatScreen> {
   bool? previousIsInChat;
   String? currentChatExecutionId;
   bool stopUserLeftListener = false;
+
+  // Voice recording (WhatsApp-style) — max 60 seconds
+  static const int _maxVoiceDurationSeconds = 60;
+  final AudioRecorder _audioRecorder = AudioRecorder();
+  bool _isRecording = false;
+  int _recordSeconds = 0;
+  Timer? _recordTimer;
+  String? _currentRecordPath;
+
+  // Voice message playback
+  final AudioPlayer _voicePlayer = AudioPlayer();
+  String? _playingVoiceUrl;
+
   @override
   void dispose() {
+    _recordTimer?.cancel();
+    if (_isRecording) {
+      _audioRecorder.stop().ignore();
+    }
+    _audioRecorder.dispose();
+    _voicePlayer.dispose();
     if (chatController.countdownController != null) {
       chatController.countdownController!.dispose();
       chatController.countdownController = null;
@@ -135,6 +158,11 @@ class _AcceptChatScreenState extends State<AcceptChatScreen> {
   @override
   void initState() {
     super.initState();
+    _voicePlayer.onPlayerComplete.listen((_) {
+      if (mounted) {
+        setState(() => _playingVoiceUrl = null);
+      }
+    });
 
     if (widget.flagId != 0) {
       _isEndChatExecuted = false;
@@ -390,6 +418,244 @@ class _AcceptChatScreenState extends State<AcceptChatScreen> {
     bottomNavigationController.update();
   }
 
+  Future<bool> _requestMicPermission() async {
+    if (await Permission.microphone.isGranted) return true;
+    final status = await Permission.microphone.request();
+    return status.isGranted;
+  }
+
+  Future<void> _startVoiceRecording() async {
+    if (_isRecording) return;
+    final hasPermission = await _requestMicPermission();
+    if (!hasPermission) {
+      global.showToast(
+        message: 'Microphone permission is required to record voice messages',
+        textColor: Colors.white,
+        bgColor: Colors.red,
+      );
+      return;
+    }
+    try {
+      final dir = await getTemporaryDirectory();
+      final path = '${dir.path}/voice_${DateTime.now().millisecondsSinceEpoch}.m4a';
+      final canRecord = await _audioRecorder.hasPermission();
+      if (!canRecord) {
+        global.showToast(
+          message: 'Microphone access denied',
+          textColor: Colors.white,
+          bgColor: Colors.red,
+        );
+        return;
+      }
+      await _audioRecorder.start(const RecordConfig(encoder: AudioEncoder.aacLc), path: path);
+      _currentRecordPath = path;
+      _isRecording = true;
+      _recordSeconds = 0;
+      _recordTimer?.cancel();
+      _recordTimer = Timer.periodic(const Duration(seconds: 1), (t) {
+        if (t.tick >= _maxVoiceDurationSeconds) {
+          t.cancel();
+          _recordTimer = null;
+          if (mounted) {
+            setState(() => _recordSeconds = _maxVoiceDurationSeconds);
+          }
+          _stopVoiceRecordingAndSend();
+          return;
+        }
+        if (mounted) {
+          setState(() => _recordSeconds = t.tick);
+        }
+      });
+      setState(() {});
+    } catch (e) {
+      log('Start recording error: $e');
+      global.showToast(
+        message: 'Could not start recording',
+        textColor: Colors.white,
+        bgColor: Colors.red,
+      );
+    }
+  }
+
+  Future<void> _stopVoiceRecordingAndSend() async {
+    if (!_isRecording) return;
+    _recordTimer?.cancel();
+    _recordTimer = null;
+    try {
+      final path = await _audioRecorder.stop();
+      _isRecording = false;
+      final filePath = path ?? _currentRecordPath;
+      _currentRecordPath = null;
+      setState(() {});
+      if (filePath != null && File(filePath).existsSync()) {
+        await chatController.sendVoiceMessageToFirebase(
+          widget.fireBasechatId,
+          widget.astrologerId,
+          File(filePath),
+        );
+      }
+    } catch (e) {
+      log('Stop recording error: $e');
+      _isRecording = false;
+      _currentRecordPath = null;
+      setState(() {});
+    }
+  }
+
+  Future<void> _cancelVoiceRecording() async {
+    if (!_isRecording) return;
+    _recordTimer?.cancel();
+    _recordTimer = null;
+    try {
+      await _audioRecorder.stop();
+    } catch (_) {}
+    _isRecording = false;
+    _currentRecordPath = null;
+    setState(() {});
+  }
+
+  static bool _isVoiceMessage(ChatMessageModel msg) {
+    if (msg.attachementPath == null || msg.attachementPath!.isEmpty) return false;
+    final p = msg.attachementPath!.toLowerCase();
+    return p.contains('.m4a') || p.contains('.aac') || p.contains('.mp3') ||
+        msg.message == 'Voice message';
+  }
+
+  Future<void> _toggleVoicePlay(String url) async {
+    if (_playingVoiceUrl == url) {
+      await _voicePlayer.pause();
+      setState(() => _playingVoiceUrl = null);
+    } else {
+      await _voicePlayer.play(UrlSource(url));
+      setState(() => _playingVoiceUrl = url);
+    }
+  }
+
+  Widget _buildVoiceMessageBubble(ChatMessageModel message) {
+    final url = message.attachementPath ?? '';
+    final isPlaying = _playingVoiceUrl == url;
+    return InkWell(
+      onTap: () => _toggleVoicePlay(url),
+      child: Container(
+        padding: EdgeInsets.symmetric(horizontal: 4.w, vertical: 2.h),
+        decoration: BoxDecoration(
+          color: chatController.isMe ? Color(0xFFfbf1f2) : Colors.grey.shade100,
+          borderRadius: BorderRadius.circular(12),
+        ),
+        child: Row(
+          mainAxisSize: MainAxisSize.min,
+          children: [
+            Icon(
+              isPlaying ? Icons.pause_circle_filled : Icons.play_circle_filled,
+              color: Get.theme.primaryColor,
+              size: 28.sp,
+            ),
+            SizedBox(width: 3.w),
+            Icon(Icons.mic, size: 20.sp, color: Colors.grey.shade600),
+            SizedBox(width: 2.w),
+            Text(
+              'Voice message',
+              style: TextStyle(fontSize: 13.sp, color: Colors.black87),
+            ),
+            if (message.createdAt != null)
+              Padding(
+                padding: EdgeInsets.only(left: 2.w),
+                child: Text(
+                  DateFormat().add_jm().format(message.createdAt!),
+                  style: TextStyle(color: Colors.grey, fontSize: 10.sp),
+                ),
+              ),
+          ],
+        ),
+      ),
+    );
+  }
+
+  Widget _buildRecordingStrip() {
+    final minutes = _recordSeconds ~/ 60;
+    final secs = _recordSeconds % 60;
+    final timeStr = '${minutes.toString().padLeft(2, '0')}:${secs.toString().padLeft(2, '0')}';
+    final nearLimit = _recordSeconds >= _maxVoiceDurationSeconds - 10;
+    return Container(
+      margin: EdgeInsets.fromLTRB(8, 0, 8, 1.h),
+      padding: EdgeInsets.symmetric(horizontal: 3.w, vertical: 1.2.h),
+      decoration: BoxDecoration(
+        color: Colors.grey.shade200,
+        borderRadius: BorderRadius.circular(24),
+        border: Border.all(color: Colors.grey.shade400),
+      ),
+      child: Row(
+        children: [
+          Icon(Icons.mic, color: Colors.red, size: 24.sp),
+          SizedBox(width: 3.w),
+          Expanded(
+            child: Column(
+              mainAxisSize: MainAxisSize.min,
+              crossAxisAlignment: CrossAxisAlignment.start,
+              children: [
+                Text(
+                  'Recording...',
+                  style: TextStyle(
+                    fontSize: 12.sp,
+                    color: Colors.grey.shade700,
+                    fontWeight: FontWeight.w500,
+                  ),
+                ),
+                Row(
+                  children: [
+                    Text(
+                      timeStr,
+                      style: TextStyle(
+                        fontSize: 14.sp,
+                        color: nearLimit ? Colors.orange.shade800 : Colors.black87,
+                        fontWeight: FontWeight.w600,
+                      ),
+                    ),
+                    SizedBox(width: 2.w),
+                    Text(
+                      '/ ${_maxVoiceDurationSeconds ~/ 60}:${(_maxVoiceDurationSeconds % 60).toString().padLeft(2, '0')} max',
+                      style: TextStyle(
+                        fontSize: 11.sp,
+                        color: Colors.grey.shade600,
+                      ),
+                    ),
+                  ],
+                ),
+              ],
+            ),
+          ),
+          GestureDetector(
+            onTap: _cancelVoiceRecording,
+            child: Padding(
+              padding: EdgeInsets.symmetric(horizontal: 2.w),
+              child: Text(
+                'Cancel',
+                style: TextStyle(
+                  fontSize: 13.sp,
+                  color: Colors.grey.shade700,
+                  fontWeight: FontWeight.w600,
+                ),
+              ),
+            ),
+          ),
+          Container(
+            height: 5.h,
+            width: 5.h,
+            decoration: BoxDecoration(
+              color: Get.theme.primaryColor,
+              shape: BoxShape.circle,
+            ),
+            child: IconButton(
+              padding: EdgeInsets.zero,
+              icon: Icon(Icons.send_rounded, color: Colors.white, size: 18.sp),
+              onPressed: _stopVoiceRecordingAndSend,
+            ),
+          ),
+        ],
+      ),
+    );
+  }
+
   void _initializeChatSession() {
     if (widget.flagId == 1) {
       log('_initializeChatSession chat session');
@@ -1006,7 +1272,9 @@ class _AcceptChatScreenState extends State<AcceptChatScreen> {
                                                                       )
                                                                     : messageList[index].attachementPath !=
                                                                             ""
-                                                                        ? messageList[index].attachementPath!.toLowerCase().contains('.pdf')
+                                                                        ? _isVoiceMessage(messageList[index])
+                                                                            ? _buildVoiceMessageBubble(messageList[index])
+                                                                            : messageList[index].attachementPath!.toLowerCase().contains('.pdf')
                                                                             ? InkWell(
                                                                                 onTap: () {
                                                                                   debugPrint('pdf onclicked');
@@ -1401,6 +1669,9 @@ class _AcceptChatScreenState extends State<AcceptChatScreen> {
                                       margin: EdgeInsets.fromLTRB(8, 0, 8, 1.h),
                                       child: GetBuilder<ChatController>(
                                           builder: (chatController) {
+                                        if (_isRecording) {
+                                          return _buildRecordingStrip();
+                                        }
                                         return Row(
                                           children: [
                                             Expanded(
@@ -1519,27 +1790,46 @@ class _AcceptChatScreenState extends State<AcceptChatScreen> {
                                               ),
                                             ),
                                             Padding(
-                                              padding: const EdgeInsets.only(
-                                                  left: 3.0),
+                                              padding: const EdgeInsets.only(left: 3.0),
                                               child: Container(
-                                                margin: EdgeInsets.only(
-                                                    bottom: 1.h),
+                                                margin: EdgeInsets.only(bottom: 1.h),
                                                 height: 6.h,
                                                 width: 6.h,
                                                 decoration: BoxDecoration(
                                                   color: Colors.grey.shade700,
                                                   shape: BoxShape.circle,
-                                                  border: Border.all(
-                                                    color: Colors.grey,
+                                                  border: Border.all(color: Colors.grey),
+                                                ),
+                                                child: InkWell(
+                                                  onTap: _startVoiceRecording,
+                                                  child: Padding(
+                                                    padding: EdgeInsets.only(left: 5.0),
+                                                    child: Icon(
+                                                      Icons.mic_rounded,
+                                                      size: 18.sp,
+                                                      color: Colors.white,
+                                                    ),
                                                   ),
                                                 ),
+                                              ),
+                                            ),
+                                            Padding(
+                                              padding: const EdgeInsets.only(left: 3.0),
+                                              child: Container(
+                                                margin: EdgeInsets.only(bottom: 1.h),
+                                                height: 6.h,
+                                                width: 6.h,
+                                                decoration: BoxDecoration(
+                                                  color: Colors.grey.shade700,
+                                                  shape: BoxShape.circle,
+                                                  border: Border.all(color: Colors.grey),
+                                                ),
                                                 child: InkWell(
                                                   onTap: () {
                                                     sendmessageOnTaporEnter();
                                                   },
                                                   child: Padding(
-                                                    padding: EdgeInsets.only(
-                                                        left: 5.0),
+                                                    padding: EdgeInsets.only(left: 5.0),
                                                     child: Icon(
                                                       Icons.send,
                                                       size: 18.sp,
diff --git a/linux/flutter/ephemeral/.plugin_symlinks/record_linux b/linux/flutter/ephemeral/.plugin_symlinks/record_linux
new file mode 120000
index 0000000..92a0996
--- /dev/null
+++ b/linux/flutter/ephemeral/.plugin_symlinks/record_linux
@@ -0,0 +1 @@
+/Users/yudish/.pub-cache/hosted/pub.dev/record_linux-1.3.0/
\ No newline at end of file
diff --git a/linux/flutter/generated_plugin_registrant.cc b/linux/flutter/generated_plugin_registrant.cc
index 6c73273..b390e86 100644
--- a/linux/flutter/generated_plugin_registrant.cc
+++ b/linux/flutter/generated_plugin_registrant.cc
@@ -10,6 +10,7 @@
 #include <file_selector_linux/file_selector_plugin.h>
 #include <flutter_volume_controller/flutter_volume_controller_plugin.h>
 #include <open_file_linux/open_file_linux_plugin.h>
+#include <record_linux/record_linux_plugin.h>
 #include <scroll_screenshot/scroll_screenshot_plugin.h>
 #include <syncfusion_pdfviewer_linux/syncfusion_pdfviewer_linux_plugin.h>
 #include <url_launcher_linux/url_launcher_plugin.h>
@@ -28,6 +29,9 @@ void fl_register_plugins(FlPluginRegistry* registry) {
   g_autoptr(FlPluginRegistrar) open_file_linux_registrar =
       fl_plugin_registry_get_registrar_for_plugin(registry, "OpenFileLinuxPlugin");
   open_file_linux_plugin_register_with_registrar(open_file_linux_registrar);
+  g_autoptr(FlPluginRegistrar) record_linux_registrar =
+      fl_plugin_registry_get_registrar_for_plugin(registry, "RecordLinuxPlugin");
+  record_linux_plugin_register_with_registrar(record_linux_registrar);
   g_autoptr(FlPluginRegistrar) scroll_screenshot_registrar =
       fl_plugin_registry_get_registrar_for_plugin(registry, "ScrollScreenshotPlugin");
   scroll_screenshot_plugin_register_with_registrar(scroll_screenshot_registrar);
diff --git a/linux/flutter/generated_plugins.cmake b/linux/flutter/generated_plugins.cmake
index 949d59a..18a6b2f 100644
--- a/linux/flutter/generated_plugins.cmake
+++ b/linux/flutter/generated_plugins.cmake
@@ -7,6 +7,7 @@ list(APPEND FLUTTER_PLUGIN_LIST
   file_selector_linux
   flutter_volume_controller
   open_file_linux
+  record_linux
   scroll_screenshot
   syncfusion_pdfviewer_linux
   url_launcher_linux
diff --git a/macos/Flutter/GeneratedPluginRegistrant.swift b/macos/Flutter/GeneratedPluginRegistrant.swift
index 4bafba9..e35af23 100644
--- a/macos/Flutter/GeneratedPluginRegistrant.swift
+++ b/macos/Flutter/GeneratedPluginRegistrant.swift
@@ -27,6 +27,7 @@ import just_audio
 import open_file_mac
 import package_info_plus
 import path_provider_foundation
+import record_macos
 import screen_brightness_macos
 import scroll_screenshot
 import share_plus
@@ -62,6 +63,7 @@ func RegisterGeneratedPlugins(registry: FlutterPluginRegistry) {
   OpenFilePlugin.register(with: registry.registrar(forPlugin: "OpenFilePlugin"))
   FPPPackageInfoPlusPlugin.register(with: registry.registrar(forPlugin: "FPPPackageInfoPlusPlugin"))
   PathProviderPlugin.register(with: registry.registrar(forPlugin: "PathProviderPlugin"))
+  RecordMacOsPlugin.register(with: registry.registrar(forPlugin: "RecordMacOsPlugin"))
   ScreenBrightnessMacosPlugin.register(with: registry.registrar(forPlugin: "ScreenBrightnessMacosPlugin"))
   ScrollScreenshotPlugin.register(with: registry.registrar(forPlugin: "ScrollScreenshotPlugin"))
   SharePlusMacosPlugin.register(with: registry.registrar(forPlugin: "SharePlusMacosPlugin"))
diff --git a/pubspec.lock b/pubspec.lock
index 7afb7ba..486af6c 100644
--- a/pubspec.lock
+++ b/pubspec.lock
@@ -1789,6 +1789,70 @@ packages:
       url: "https://pub.dev"
     source: hosted
     version: "1.4.0"
+  record:
+    dependency: "direct main"
+    description:
+      name: record
+      sha256: d5b6b334f3ab02460db6544e08583c942dbf23e3504bf1e14fd4cbe3d9409277
+      url: "https://pub.dev"
+    source: hosted
+    version: "6.2.0"
+  record_android:
+    dependency: transitive
+    description:
+      name: record_android
+      sha256: "3bb3c6abbcb5fc1e86719fc6f0acdee89dfe8078543b92caad11854c487e435a"
+      url: "https://pub.dev"
+    source: hosted
+    version: "1.5.0"
+  record_ios:
+    dependency: transitive
+    description:
+      name: record_ios
+      sha256: "8df7c136131bd05efc19256af29b2ba6ccc000ccc2c80d4b6b6d7a8d21a3b5a9"
+      url: "https://pub.dev"
+    source: hosted
+    version: "1.2.0"
+  record_linux:
+    dependency: transitive
+    description:
+      name: record_linux
+      sha256: c31a35cc158cd666fc6395f7f56fc054f31685571684be6b97670a27649ce5c7
+      url: "https://pub.dev"
+    source: hosted
+    version: "1.3.0"
+  record_macos:
+    dependency: transitive
+    description:
+      name: record_macos
+      sha256: f04d1547ff61ae54b4154e9726f656a17ad993f1a90f8f44bc40de94bafa072f
+      url: "https://pub.dev"
+    source: hosted
+    version: "1.2.0"
+  record_platform_interface:
+    dependency: transitive
+    description:
+      name: record_platform_interface
+      sha256: "8a81dbc4e14e1272a285bbfef6c9136d070a47d9b0d1f40aa6193516253ee2f6"
+      url: "https://pub.dev"
+    source: hosted
+    version: "1.5.0"
+  record_web:
+    dependency: transitive
+    description:
+      name: record_web
+      sha256: "7e9846981c1f2d111d86f0ae3309071f5bba8b624d1c977316706f08fc31d16d"
+      url: "https://pub.dev"
+    source: hosted
+    version: "1.3.0"
+  record_windows:
+    dependency: transitive
+    description:
+      name: record_windows
+      sha256: "223258060a1d25c62bae18282c16783f28581ec19401d17e56b5205b9f039d78"
+      url: "https://pub.dev"
+    source: hosted
+    version: "1.0.7"
   responsive_sizer:
     dependency: "direct main"
     description:
diff --git a/pubspec.yaml b/pubspec.yaml
index 27b9d70..be8aadf 100644
--- a/pubspec.yaml
+++ b/pubspec.yaml
@@ -90,6 +90,7 @@ dependencies:
   lottie: ^3.3.1
   sms_autofill: ^2.3.0
   razorpay_flutter: ^1.3.7
+  record: ^6.2.0
 
   # audio video calling libs
 
